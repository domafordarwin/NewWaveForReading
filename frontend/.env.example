# API Configuration
VITE_API_URL=http://localhost:8080/api

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# OpenAI API Key (Optional - if not set, simulation mode will be used)
#VITE_OPENAI_API_KEY=your-openai-api-key-here

# ========================================
# Vercel 배포 시 환경 변수 설정 방법
# ========================================
# 1. Vercel Dashboard → Your Project → Settings → Environment Variables
# 2. 위의 모든 변수를 Production, Preview, Development 환경에 추가
# 3. VITE_ 접두사는 클라이언트 번들에 포함됩니다 (보안 주의)
# 4. 재배포 후 /diagnostics 페이지에서 확인 가능
#
# 보안 참고:
# - VITE_ 접두사 변수는 브라우저에서 접근 가능합니다
# - OpenAI API 키는 프로덕션에서 백엔드 프록시를 통해 관리하는 것이 권장됩니다
